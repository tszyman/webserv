# This is the main server block
server {
    listen 8080; # Listen on port 8080
    server_name localhost;

    # Image location
    location /images {
        root /var/www/data;
        allow_methods GET DELETE; # Only these are allowed
    }
}